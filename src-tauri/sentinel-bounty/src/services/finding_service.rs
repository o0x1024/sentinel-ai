//! Finding management service - placeholder for now

pub struct FindingService;

impl FindingService {
    pub fn new() -> Self {
        Self
    }
}

impl Default for FindingService {
    fn default() -> Self {
        Self::new()
    }
}
