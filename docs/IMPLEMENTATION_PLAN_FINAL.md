# 🚀 AI驱动被动扫描自动化 - 最终实施计划

> **重大发现**: Playwright MCP 已集成！开发周期从 1周 缩短至 **3-4天**！

---

## 📊 当前状态

### ✅ 已完成 (95%)

| 模块 | 状态 | 说明 |
|-----|------|------|
| 被动扫描系统 | ✅ 100% | Phase 1-6 完成，Hudsucker代理+插件引擎+数据库 |
| 浏览器自动化 | ✅ 100% | **Playwright MCP 已集成（30+ 工具）** |
| AI助手系统 | ✅ 100% | ReAct/Plan-Execute多架构，工具调用 |
| 实时事件推送 | ✅ 100% | Tauri Events完备 |
| 前端UI | ✅ 100% | 被动扫描控制+漏洞看板+AI对话 |

### ⚠️ 待完成 (5%)

| 模块 | 优先级 | 工作量 | 说明 |
|-----|-------|--------|------|
| 被动扫描MCP工具封装 | P0 | 1天 | 包装现有Tauri命令 |
| 插件模板库 | P0 | 0.5天 | 5-10个模板 |
| AI插件生成逻辑 | P0 | 0.5天 | 模板选择+参数填充 |
| AI工作流编排 | P0 | 1天 | 工具调用序列设计 |
| 实时UI增强 | P1 | 0.5天 | 可选 |

---

## 🎯 3天MVP实施计划

### Day 1: 被动扫描MCP工具封装

#### 任务清单
- [ ] 创建 `src-tauri/src/tools/passive_scan_control.rs`
- [ ] 实现 MCP 工具：
  ```rust
  - start_passive_scan(port?, max_request_size?, max_response_size?)
  - stop_passive_scan()
  - get_passive_scan_status()
  - list_findings(vuln_type?, severity?, limit?, offset?)
  - get_finding_detail(finding_id)
  - load_plugin(code, metadata)
  - enable_plugin(plugin_id)
  - disable_plugin(plugin_id)
  ```
- [ ] 注册到统一工具管理器
- [ ] 测试AI助手调用

#### 预计时间：6-8小时

---

### Day 2: 插件模板库 + 生成逻辑

#### 任务清单

**Part 1: 创建插件模板库** (3小时)

创建 `src-tauri/sentinel-plugins/templates/` 目录，包含以下模板：

1. **SQL注入检测模板** (`sqli_template.ts`)
   - 检测参数中的SQL特殊字符
   - 检测响应中的数据库错误
   - 可配置参数：目标参数名、数据库类型

2. **XSS检测模板** (`xss_template.ts`)
   - 反射型XSS检测
   - 存储型XSS检测
   - 可配置参数：目标参数名、危险标签列表

3. **越权检测模板** (`auth_bypass_template.ts`)
   - ID参数遍历检测
   - 权限绕过检测
   - 可配置参数：ID参数名、测试范围

4. **敏感信息泄露模板** (`info_leak_template.ts`)
   - API Key泄露
   - 敏感路径访问
   - 可配置参数：敏感词列表、路径列表

5. **CSRF检测模板** (`csrf_template.ts`)
   - Token缺失检测
   - 可配置参数：表单endpoint

**Part 2: 实现插件生成工具** (3小时)

创建 `src-tauri/src/tools/plugin_generator.rs`：

```rust
pub struct PluginGeneratorTool;

impl PluginGeneratorTool {
    // 从模板生成插件
    pub async fn generate_from_template(
        template_name: &str,  // "sqli", "xss", "auth_bypass"
        target_url: &str,      // "https://zeus.imgo.tv"
        params: HashMap<String, Value>  // 自定义参数
    ) -> Result<GeneratedPlugin> {
        // 1. 加载模板文件
        // 2. 使用LLM分析目标URL和参数
        // 3. 填充模板参数
        // 4. 生成唯一的plugin_id
        // 5. 保存到数据库
        // 6. 返回生成的插件信息
    }
}
```

#### 预计时间：6-8小时

---

### Day 3: AI工作流编排 + 端到端测试

#### 任务清单

**Part 1: 设计AI工作流Prompt** (2小时)

创建 `src-tauri/src/prompts/auto_security_test.md`：

```markdown
# 自动化安全测试工作流

当用户请求测试网站安全风险时，按以下步骤执行：

## 步骤1: 启动被动扫描
工具: start_passive_scan
参数: { port: 4201, max_request_size: 2097152, max_response_size: 2097152 }

## 步骤2: 启动浏览器并配置代理
工具: playwright_navigate
参数: { 
  url: <目标URL>, 
  browserType: "chromium",
  headless: false,
  // 关键：配置代理
  // Playwright MCP会自动支持代理配置
}

## 步骤3: 分析网站结构
工具: playwright_get_visible_html
分析内容：
- 表单输入框 (input, textarea)
- 搜索功能
- 用户资料/详情页（带ID参数）
- 评论/留言功能

## 步骤4: 生成针对性插件
根据用户需求和网站特征，调用 plugin_generator 工具：

- 如果有搜索/表单 → 生成 sqli 插件
- 如果有输入框/评论 → 生成 xss 插件
- 如果有ID参数 → 生成 auth_bypass 插件
- 如果检测到敏感信息 → 生成 info_leak 插件

## 步骤5: 自动化交互测试
使用 Playwright 工具进行交互：

测试场景1: 搜索功能
- playwright_fill: 填充SQL注入payload
- playwright_click: 提交表单

测试场景2: 评论功能
- playwright_fill: 填充XSS payload
- playwright_click: 提交评论

测试场景3: 用户资料
- playwright_navigate: 遍历ID参数

## 步骤6: 收集结果
工具: list_findings
参数: { limit: 100 }

## 步骤7: 生成报告
总结发现的漏洞，按严重程度排序

## 步骤8: 清理
- playwright_close: 关闭浏览器
- (可选) stop_passive_scan: 停止扫描
```

**Part 2: 实现工作流编排器** (2小时)

创建 `src-tauri/src/tools/auto_security_test.rs`：

```rust
pub struct AutoSecurityTestTool;

impl UnifiedTool for AutoSecurityTestTool {
    async fn execute(&self, params: ToolExecutionParams) -> Result<ToolExecutionResult> {
        let url = params.inputs.get("url").unwrap().as_str().unwrap();
        let vuln_types = params.inputs.get("vuln_types"); // ["sqli", "xss"]
        
        let mut report = TestReport::new(url);
        
        // 1. 启动被动扫描
        report.add_step("启动被动扫描...");
        let port = self.start_passive_scan().await?;
        
        // 2. 启动浏览器
        report.add_step("启动浏览器...");
        self.launch_browser_with_proxy(url, port).await?;
        
        // 3. 分析网站
        report.add_step("分析网站结构...");
        let site_info = self.analyze_website().await?;
        
        // 4. 生成插件
        report.add_step("生成检测插件...");
        let plugins = self.generate_plugins(&site_info, vuln_types).await?;
        
        // 5. 自动化测试
        report.add_step("执行自动化测试...");
        self.run_automated_tests(&site_info).await?;
        
        // 6. 收集结果
        report.add_step("收集测试结果...");
        let findings = self.collect_findings().await?;
        
        // 7. 清理
        self.cleanup().await?;
        
        report.set_findings(findings);
        Ok(ToolExecutionResult::success(report.to_json()))
    }
}
```

**Part 3: 端到端测试** (2小时)

创建测试脚本 `tests/auto_security_test_e2e.rs`：

```rust
#[tokio::test]
async fn test_full_auto_security_workflow() {
    // 1. 准备测试环境
    let test_url = "http://testphp.vulnweb.com/"; // 公开测试靶场
    
    // 2. 调用自动化测试工具
    let tool = AutoSecurityTestTool::new();
    let result = tool.execute(ToolExecutionParams {
        inputs: hashmap! {
            "url" => json!(test_url),
            "vuln_types" => json!(["sqli", "xss"])
        }
    }).await.unwrap();
    
    // 3. 验证结果
    assert!(result.success);
    let report: TestReport = serde_json::from_value(result.output).unwrap();
    assert!(report.findings.len() > 0);
    assert!(report.findings.iter().any(|f| f.vuln_type == "sqli"));
}
```

#### 预计时间：6-8小时

---

## 🎬 完整工作流示例

### 用户输入
```
打开被动扫描，测试 https://zeus.imgo.tv/ 是否存在SQL注入和XSS漏洞
```

### AI助手执行过程

```
[1/8] 启动被动扫描...
  → 工具: start_passive_scan()
  ✅ 代理已启动，监听端口: 4201

[2/8] 启动浏览器...
  → 工具: playwright_navigate(url="https://zeus.imgo.tv/", proxy="127.0.0.1:4201")
  ✅ Chrome浏览器已启动（代理已配置）
  ✅ 页面加载完成

[3/8] 分析网站结构...
  → 工具: playwright_get_visible_html()
  📊 发现 3 个搜索框
  📊 发现 2 个评论表单
  📊 发现 1 个用户资料页（ID参数）

[4/8] 生成检测插件...
  → 工具: generate_plugin(template="sqli", target_params=["q", "search"])
  ✅ 已生成: zeus_sqli_detector (ID: auto_gen_sqli_20251112_143022)
  
  → 工具: generate_plugin(template="xss", target_params=["comment", "content"])
  ✅ 已生成: zeus_xss_detector (ID: auto_gen_xss_20251112_143023)
  
  → 工具: load_plugin(plugin_id="auto_gen_sqli_20251112_143022")
  → 工具: enable_plugin(plugin_id="auto_gen_sqli_20251112_143022")
  ✅ 插件已加载到扫描引擎

[5/8] 执行自动化测试...
  
  🔍 测试场景 1/3: 搜索功能 SQL注入
    → playwright_fill(selector="input[name='q']", value="test' OR 1=1--")
    → playwright_click(selector="button[type='submit']")
    → 等待被动扫描检测...
    ⚠️ 发现漏洞: SQL注入 (参数 'q') - CRITICAL
  
  🔍 测试场景 2/3: 评论功能 XSS
    → playwright_fill(selector="textarea[name='comment']", value="<script>alert(1)</script>")
    → playwright_click(selector=".submit-comment")
    → 等待被动扫描检测...
    ⚠️ 发现漏洞: 存储型XSS (评论功能) - HIGH
  
  🔍 测试场景 3/3: 用户资料 越权
    → playwright_navigate(url="/user/profile?id=123")
    → playwright_navigate(url="/user/profile?id=456")
    ✅ 无越权漏洞发现

[6/8] 收集测试结果...
  → 工具: list_findings(limit=100)
  📊 共发现 2 个漏洞

[7/8] 生成测试报告...
  📄 报告已生成: ~/sentinel-ai/reports/zeus_imgo_tv_20251112_143522.html

[8/8] 清理环境...
  → 工具: playwright_close()
  ✅ 浏览器已关闭
  ✅ 被动扫描继续运行（供手动测试使用）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 测试报告摘要

目标网站: https://zeus.imgo.tv/
测试时长: 2分15秒
测试场景: 3个
请求总数: 38
发现漏洞: 2个

🔴 Critical (1):
  1. SQL注入 @ /search?q=test%27+OR+1%3D1--
     - 参数: q
     - 证据: MySQL syntax error in response
     - CWE: CWE-89
     - OWASP: A03:2021

🟠 High (1):
  2. 存储型XSS @ /comments
     - 参数: comment
     - 证据: <script>alert(1)</script> reflected without encoding
     - CWE: CWE-79
     - OWASP: A03:2021

详细报告: ~/sentinel-ai/reports/zeus_imgo_tv_20251112_143522.html

需要我对某个漏洞进行深入分析或生成PoC吗？
```

---

## 🔧 关键技术实现

### 1. Playwright MCP 代理配置

Playwright MCP 工具需要支持代理配置。查看当前实现，可能需要扩展参数：

```typescript
// playwright MCP 工具需要支持的参数
playwright_navigate({
  url: "https://zeus.imgo.tv/",
  browserType: "chromium",
  headless: false,
  // 关键：代理配置
  proxyServer: "http://127.0.0.1:4201"
})
```

**如果 Playwright MCP 不直接支持代理配置，需要：**
1. 确认 Playwright MCP 服务器的启动参数
2. 在启动 Playwright 时全局配置代理
3. 或者在每次 navigate 时通过浏览器上下文配置代理

### 2. 被动扫描与浏览器同步

关键点：确保浏览器请求通过被动扫描代理

**验证方法**：
```rust
// 在被动扫描代理中添加日志
info!("Received request: {} {}", method, uri);

// 在 AI 工作流中检查
let stats = get_passive_scan_status().await?;
if stats.requests == 0 {
    warn!("浏览器流量未经过代理！");
}
```

### 3. 插件动态加载时机

插件需要在浏览器测试**之前**加载：

```rust
// 正确顺序
1. start_passive_scan()        // 启动代理
2. generate_plugin()            // 生成插件
3. load_plugin() + enable_plugin()  // 加载插件
4. playwright_navigate()        // 开始测试 ← 此时插件已在监听
```

---

## 📊 验收标准

### 功能验收
- [ ] AI可自动启动被动扫描
- [ ] AI可自动启动浏览器并配置代理
- [ ] AI可自动访问目标网站
- [ ] AI可根据需求生成插件（至少2种类型）
- [ ] 插件可正确检测漏洞
- [ ] 实时显示测试进度和发现的漏洞
- [ ] 生成HTML测试报告

### 性能验收
- [ ] 完整测试流程 < 5分钟
- [ ] 浏览器启动 < 3秒
- [ ] 插件生成 < 10秒
- [ ] 无内存泄漏

### 用户体验验收
- [ ] AI响应流畅，实时反馈
- [ ] 错误处理友好（网络错误、超时等）
- [ ] 可中途取消测试
- [ ] 报告清晰易读

---

## 🚨 风险和缓解

### 风险1: Playwright MCP 代理配置问题
**概率**: 中  
**影响**: 高（核心功能）  
**缓解**:
- 提前测试 Playwright MCP 代理配置
- 如不支持，联系 MCP 服务器开发者或修改源码
- 备选：使用系统代理配置

### 风险2: 插件生成质量不稳定
**概率**: 高  
**影响**: 中（可迭代优化）  
**缓解**:
- 使用模板化生成（降低LLM依赖）
- 添加代码验证逻辑
- 提供人工审核界面

### 风险3: 目标网站反爬虫
**概率**: 中  
**影响**: 低（特定场景）  
**缓解**:
- 使用真实浏览器（Playwright）
- 添加随机延迟
- 支持自定义 User-Agent

---

## 📅 时间规划

| 天数 | 任务 | 交付物 | 状态 |
|-----|------|--------|------|
| Day 1 | 被动扫描MCP工具封装 | 8个MCP工具 | ⏳ 待开始 |
| Day 2 | 插件模板库 + 生成逻辑 | 5个模板 + 生成器 | ⏳ 待开始 |
| Day 3 | AI工作流编排 + 测试 | 完整工作流 + E2E测试 | ⏳ 待开始 |
| Day 4 | 实时UI增强（可选） | 进度卡片 + 优化 | ⏳ 可选 |

**总工作量**: 24-32小时  
**建议排期**: 连续3-4个工作日

---

## ✅ 完成标志

当以下所有条件满足时，MVP即为完成：

1. ✅ 用户对AI说："测试 XXX 网站的安全风险"
2. ✅ AI自动完成完整测试流程（无需手动干预）
3. ✅ 发现至少1个真实漏洞（在测试靶场验证）
4. ✅ 生成HTML测试报告
5. ✅ 整个过程 < 5分钟
6. ✅ 无崩溃或严重错误

---

## 🎉 后续扩展方向

完成MVP后，可选的扩展方向（按优先级排序）：

### 短期（1-2周）
1. **更多插件模板** (15+ 种)
   - SSRF、CORS、IDOR、XXE、命令注入等
   
2. **智能测试策略**
   - 根据网站类型选择测试场景
   - 自动识别登录流程

3. **增强报告**
   - 漏洞复现步骤
   - 修复建议和代码示例
   - PoC代码生成

### 中期（1-2个月）
4. **高级AI插件生成**
   - 完整的AI代码生成（替代模板）
   - 插件质量评分系统

5. **网站结构深度分析**
   - API端点自动发现
   - 认证流程自动识别
   - 业务逻辑分析

6. **分布式测试**
   - 多浏览器并发测试
   - 任务队列和调度

### 长期（3-6个月）
7. **主动扫描集成**
   - 集成 Nuclei/XRAY
   - 漏洞验证和利用

8. **移动应用测试**
   - Android/iOS 支持
   - App流量拦截

9. **社区生态**
   - 插件市场
   - 漏洞知识库

---

## 📚 参考资料

- [主计划文档](./ai_driven_passive_scan_automation_plan.md)
- [技术选型文档](./browser_automation_tech_comparison.md)
- [路线图总结](./implementation_roadmap_summary.md)
- [被动扫描实施计划](./passive_scan_implementation_plan.md)
- [Playwright MCP 工具列表](../src-tauri/logs/llm-http-requests-2025-11-12.log)

---

**最后更新**: 2025-11-12  
**版本**: 2.0 (重大更新 - Playwright MCP 已集成)  
**状态**: ✅ 计划完成，等待执行

