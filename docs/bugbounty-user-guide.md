# Bug Bounty 模块使用指南

## 概述

Bug Bounty 模块是一个完整的漏洞赏金项目管理系统，采用 **ASM（攻击面管理）为核心 + 工作流自动化执行 + 证据报告工程化输出** 的架构设计。

### 核心价值

1. **资产管理**：自动发现、归并、标记高价值资产
2. **变更监控**：实时监控目标变化，发现攻击窗口
3. **自动化测试**：工作流模板一键执行安全测试
4. **证据管理**：自动采集证据，一键生成提交报告
5. **运营跟踪**：提交时间线、复测提醒、状态追踪

---

## 功能模块说明

### 1. Program（项目管理）

**作用**：管理参与的 Bug Bounty 项目

| 字段 | 说明 |
|------|------|
| name | 项目名称（如 "HackerOne - Example Corp"） |
| organization | 目标组织 |
| platform | 平台（hackerone/bugcrowd/intigriti/private） |
| status | 状态（active/paused/closed） |
| rewards_json | 奖励范围配置 |

**使用场景**：
- 管理多个 Bug Bounty 项目
- 追踪每个项目的产出和收益
- 按项目组织资产和发现

---

### 2. Scope（范围管理）

**作用**：定义项目的测试范围

| scope_type | 说明 |
|------------|------|
| domain | 单个域名 (example.com) |
| wildcard | 通配符域名 (*.example.com) |
| url | 具体 URL |
| ip | IP 地址 |
| ip_range | IP 范围 |

**使用场景**：
- 定义测试边界，避免越权测试
- 区分 in-scope 和 out-of-scope
- 为资产自动关联提供依据

---

### 3. Asset（资产管理）🔑

**作用**：管理发现的所有资产（URL、子域名、IP等）

**核心特性**：
- **URL 规范化**：自动去重合并相同资产
- **指纹识别**：记录技术栈信息
- **标签体系**：标记高价值资产
- **优先级评分**：智能排序测试目标

**高价值标签**：
| 标签 | 说明 | 评分权重 |
|------|------|----------|
| admin-panel | 管理后台 | 3.0 |
| payment-gateway | 支付接口 | 3.0 |
| vulnerable-tech | 已知漏洞技术 | 3.0 |
| user-data | 用户数据接口 | 2.5 |
| auth-system | 认证系统 | 2.5 |
| exposed-config | 暴露的配置 | 2.5 |
| file-upload | 文件上传 | 2.0 |
| debug-enabled | 调试模式 | 2.0 |
| api-endpoint | API 接口 | 1.5 |

**优先级评分算法**：
```
score = 标签权重之和 + 技术栈复杂度(max 2.0) + 历史发现数(max 3.0) + 变更频率(max 2.0)
if 有 vulnerable-tech 标签: score *= 1.2
最终分数归一化到 0-10
```

---

### 4. Change Event（变更监控）🔔

**作用**：监控资产变化，发现攻击窗口

**事件类型**：
| 类型 | 说明 | 典型场景 |
|------|------|----------|
| dns_change | DNS 记录变化 | 新增子域名、IP 变更 |
| certificate_change | SSL 证书变化 | 新证书、即将过期 |
| content_change | 内容变化 | 页面更新、新功能上线 |
| technology_change | 技术栈变化 | 框架升级/降级 |
| api_change | API 变化 | 新接口、参数变更 |
| asset_discovered | 新资产发现 | 新子域名、新服务 |

**严重级别**：critical > high > medium > low > info

**自动触发工作流**：
当变更事件满足条件时，自动触发绑定的工作流进行测试。

---

### 5. Workflow Template（工作流模板）⚡

**作用**：预定义的自动化测试流程

**内置模板**：

| 模板 | 用途 | 步骤 |
|------|------|------|
| Subdomain Recon Pipeline | 子域名发现 | 枚举 → DNS解析 → HTTP探测 → 指纹识别 |
| Directory Discovery | 目录发现 | 目录爆破 → 敏感文件检测 |
| Basic Vulnerability Scan | 基础漏洞扫描 | XSS检测 → SQLi检测 → 敏感信息检测 |
| API Endpoint Discovery | API 发现 | Swagger检测 → API模糊测试 |
| Full Recon Pipeline | 完整侦察 | 上述所有步骤的组合 |

**模板绑定**：
- 将模板绑定到 Program 或 Scope
- 配置自动触发条件
- 设置定时执行计划

---

### 6. Finding（发现管理）🎯

**作用**：记录发现的安全问题

**关键字段**：
| 字段 | 说明 |
|------|------|
| title | 漏洞标题 |
| finding_type | 类型（xss/sqli/idor/ssrf 等） |
| severity | 严重程度（critical/high/medium/low） |
| affected_url | 受影响 URL |
| affected_parameter | 受影响参数 |
| fingerprint | 去重指纹（自动生成） |
| reproduction_steps | 复现步骤 |
| impact | 影响说明 |
| remediation | 修复建议 |

**去重机制**：
```
fingerprint = MD5(program_id + vuln_type + affected_url + affected_parameter)
```
相同指纹的发现会自动合并，避免重复记录。

---

### 7. Evidence（证据管理）📎

**作用**：管理漏洞相关的证据材料

**证据类型**：
| 类型 | 说明 |
|------|------|
| screenshot | 截图 |
| http_request | HTTP 请求 |
| http_response | HTTP 响应 |
| poc | PoC 代码 |
| video | 视频录制 |
| log | 日志文件 |

**自动采集**：
- Traffic 流量分析插件检测到漏洞时自动生成
- Workflow 执行过程中自动保存
- 手动上传补充

---

### 8. Submission（提交管理）📤

**作用**：管理向平台的漏洞提交

**状态流转**：
```
draft → submitted → triaged → accepted/rejected → closed
                  ↓
            needs_more_info
```

**时间线跟踪**：
- 记录每次沟通
- 标记重要节点
- 自动提醒跟进

---

## 典型使用流程

### 流程 1：新项目启动

```
1. 创建 Program
   ↓
2. 添加 Scope（定义测试范围）
   ↓
3. 从 Scope 导入 Assets（自动规范化）
   ↓
4. 绑定 Workflow 模板（配置自动化）
   ↓
5. 执行初始侦察（Full Recon Pipeline）
```

### 流程 2：日常监控

```
1. Change Event 检测到变化
   ↓
2. 评估严重程度和风险分数
   ↓
3. 自动/手动触发 Workflow
   ↓
4. Workflow 执行，产出 Finding + Evidence
   ↓
5. 审核 Finding，准备 Submission
```

### 流程 3：漏洞提交

```
1. 选择 Finding
   ↓
2. 补充/整理 Evidence
   ↓
3. 一键导出报告（Markdown/HTML）
   ↓
4. 创建 Submission 记录
   ↓
5. 跟踪时间线，等待回复
   ↓
6. 复测确认（Schedule Retest）
```

---

## 模块配合关系图

```
┌─────────────┐
│   Program   │ ──────────────────────────────────────┐
└──────┬──────┘                                       │
       │ 1:N                                          │
       ▼                                              │
┌─────────────┐      导入资产      ┌─────────────┐   │
│    Scope    │ ───────────────► │    Asset    │   │
└──────┬──────┘                   └──────┬──────┘   │
       │                                 │          │
       │ 绑定模板                        │ 变更监控  │
       ▼                                 ▼          │
┌─────────────┐      触发       ┌─────────────┐    │
│  Workflow   │ ◄────────────── │ChangeEvent │    │
│  Template   │                 └─────────────┘    │
└──────┬──────┘                                     │
       │ 执行产出                                   │
       ▼                                            │
┌─────────────┐                 ┌─────────────┐    │
│   Finding   │ ◄───────────────│  Evidence   │    │
└──────┬──────┘    自动采集     └─────────────┘    │
       │                                            │
       │ 准备提交                                   │
       ▼                                            │
┌─────────────┐      关联项目    ───────────────────┘
│ Submission  │ ◄──────────────
└─────────────┘
```

---

## API 命令速查

### Program 操作
```typescript
// 创建项目
invoke('bounty_create_program', { request: { name, organization, platform, ... } })

// 列出项目
invoke('bounty_list_programs', { status: 'active' })
```

### Asset 操作
```typescript
// 创建/合并资产
invoke('bounty_create_asset', { request: { program_id, url, tags } })

// 从 Scope 批量导入
invoke('bounty_import_assets_from_scope', { programId, scopeId })

// 更新指纹和技术栈
invoke('bounty_update_asset_fingerprint', { request: { asset_id, tech_stack, ip_addresses } })

// 添加标签
invoke('bounty_add_asset_labels', { request: { asset_id, labels: ['admin-panel'] } })

// 获取高优先级资产
invoke('bounty_get_priority_queue', { programId, limit: 20 })
```

### Change Event 操作
```typescript
// 创建变更事件
invoke('bounty_create_change_event', { request: { program_id, asset_id, event_type, ... } })

// 列出变更事件
invoke('bounty_list_change_events', { filter: { programId, status: 'new' } })

// 触发关联工作流
invoke('bounty_trigger_workflows_for_event', { eventId })
```

### Workflow 操作
```typescript
// 初始化内置模板
invoke('bounty_init_builtin_templates')

// 列出模板
invoke('bounty_list_workflow_templates', { category: 'recon' })

// 绑定模板到项目
invoke('bounty_create_workflow_binding', { request: { program_id, workflow_template_id, auto_run_on_change: true } })
```

### Finding 操作
```typescript
// 创建发现
invoke('bounty_create_finding', { request: { program_id, title, finding_type, severity, ... } })

// 从流量导入
invoke('bounty_import_traffic_finding', { request: { program_id, vulnerability: trafficVuln } })
```

### Evidence 操作
```typescript
// 创建证据
invoke('bounty_create_evidence', { request: { finding_id, evidence_type, title, content } })
```

### Submission 操作
```typescript
// 创建提交
invoke('bounty_create_submission', { request: { program_id, finding_id, title, ... } })

// 添加时间线事件
invoke('bounty_add_submission_timeline_event', { request: { submission_id, event_type: 'response', content: '...' } })

// 获取需要跟进的提交
invoke('bounty_get_submissions_needing_followup', { programId, daysThreshold: 7 })

// 安排复测
invoke('bounty_schedule_retest', { request: { submission_id, finding_id } })
```

### 报告导出
```typescript
// 导出报告
invoke('bounty_export_report', { 
  request: { 
    finding_ids: ['...'], 
    format: 'markdown', // 'markdown' | 'html' | 'json'
    language: 'en', // 'en' | 'zh'
    include_evidence: true 
  } 
})
```

---

## 最佳实践

### 1. 资产管理
- 定期执行 `bounty_recalculate_all_asset_priorities` 更新优先级
- 使用高价值标签标记重点资产
- 关注 Priority Queue 中的 Top 20 资产

### 2. 变更监控
- 为关键资产启用自动触发工作流
- 设置合适的触发条件避免误报
- 及时处理 `new` 状态的变更事件

### 3. 工作流使用
- 从内置模板开始，逐步定制
- 绑定到 Program 级别实现全局自动化
- 绑定到 Scope 级别实现精细控制

### 4. 证据管理
- 发现漏洞时立即采集证据
- 使用 Traffic 插件自动采集
- 保持 HTTP 请求/响应的完整性

### 5. 提交运营
- 7 天无回复发送跟进
- 14 天无更新请求状态
- 修复后及时安排复测

---

## 数据库表结构

| 表名 | 说明 |
|------|------|
| bounty_programs | 项目信息 |
| bounty_scopes | 测试范围 |
| bounty_assets | 资产信息 |
| bounty_findings | 发现记录 |
| bounty_evidence | 证据材料 |
| bounty_submissions | 提交记录 |
| bounty_change_events | 变更事件 |
| bounty_workflow_templates | 工作流模板 |
| bounty_workflow_bindings | 模板绑定关系 |
