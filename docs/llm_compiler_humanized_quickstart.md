# LLMCompiler 拟人化Prompt - 快速开始

## 🚀 快速启动

### 1. 安装Prompt模板

```bash
# 进入项目目录
cd /Users/a1024/code/ai/sentinel-ai

# 导入prompt到数据库（根据你的数据库路径调整）
sqlite3 src-tauri/sentinel-ai.db < docs/llm_compiler_humanized_prompts.sql
```

### 2. 验证安装

```bash
# 检查prompt是否已安装
sqlite3 src-tauri/sentinel-ai.db "SELECT id, title, enabled FROM prompt_templates WHERE architecture = 'LLMCompiler';"
```

应该看到两条记录:
- `llmcompiler_planner_v1` - Planner阶段的拟人化prompt
- `llmcompiler_joiner_v1` - Joiner阶段的拟人化prompt

### 3. 编译运行

```bash
cd src-tauri
cargo build
```

### 4. 使用示例

在AI助手界面中:

1. **选择LLMCompiler引擎** (如果有多个引擎选项)
2. **输入任务**, 例如:
   ```
   扫描example.com的安全漏洞
   ```
3. **观察思考过程**:
   - 你会看到AI的思考过程(标记为[THINKING])
   - 然后是具体的执行计划
   - 最后是分析决策过程

## ✨ 效果对比

### 之前（冷冰冰的机器人）
```
生成执行计划...
任务1: DNS解析
任务2: 端口扫描
...
执行完成
```

### 现在（拟人化的助手）
```
[THINKING]
好的,让我想想如何处理这个任务...

用户想要扫描网站的安全漏洞,这意味着我需要:
1. 先获取目标的基本信息(IP地址)
2. 然后扫描开放的端口和服务
3. 最后针对发现的服务进行漏洞检测

让我设计一个高效的并行执行计划...

[PLAN]
{
  "tasks": [...],
  "execution_strategy": "先获取IP,然后并行扫描端口和子域名"
}
```

## 📝 Prompt内容

### Planner Prompt特点

- ✅ 强调**思考优先**,不急于给出答案
- ✅ 使用**口语化表达**,像和朋友讨论
- ✅ **解释每个任务**的原因和目的
- ✅ 展示**并行执行**的规划思路

### Joiner Prompt特点

- ✅ **深度分析**执行结果
- ✅ **诚实评估**信息是否充分
- ✅ **清晰决策**:完成还是继续
- ✅ 给用户**友好的回答**

## 🔧 自定义Prompt

如果你想调整助手的风格:

1. 编辑 `docs/llm_compiler_humanized_prompts.sql`
2. 修改prompt的`content`字段
3. 重新导入:
   ```bash
   sqlite3 src-tauri/sentinel-ai.db < docs/llm_compiler_humanized_prompts.sql
   ```

## 🐛 故障排除

### AI没有按格式响应?

**可能原因**: 使用的LLM模型能力不足

**解决方案**:
- 使用更强大的模型(如GPT-4, Claude 3, Qwen等)
- 在prompt中添加更多示例

### 看不到思考过程?

**检查项**:
1. 确认prompt已正确安装到数据库
2. 确认前端正确处理`ChunkType::Thinking`类型消息
3. 查看浏览器控制台是否有错误

### JSON解析失败?

**不用担心**: 代码中已实现多种容错机制
- 自动清理markdown标记
- 支持多种JSON格式
- 自动修复常见错误

## 📚 详细文档

更多信息请参阅:
- [完整使用指南](./llm_compiler_humanized_guide.md)
- [Prompt架构设计](./prompt_architecture_update.md)

## 🎯 核心改进

1. **思考过程可见化**: 用户能看到AI的推理过程
2. **拟人化表达**: 自然、友好的对话风格
3. **流式输出**: 实时展示进展,不用等待
4. **智能决策**: 明确解释为什么做某个选择

让你的AI助手从工具变成伙伴! 🤖➡️👨‍🎓

